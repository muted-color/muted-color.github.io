# Repository contract

이 저장소는 Jekyll 연구 블로그다. 요청한 결과와 기존 파일의 의도를 기준으로 최소 범위를 수정한다.

## 실행과 소통

- 요청된 수정은 구현과 관련 검증까지 끝낸다. 일반적인 가역적 선택은 문맥으로 결정하고, 결과를 바꿀 중요한 정보만 질문한다. 답이 필요 없는 작업은 계속한다.
- 시스템·개발자 지침을 지키며, 사용자의 명시적 요청과 이미 받은 승인은 저장소·스킬의 기본 지침보다 우선한다. 스킬은 작업 범위나 외부 실행 권한을 추가하지 않는다.
- 지침 때문에 멈춰야 한다면 정확한 파일 링크와 해당 문구, 적용 이유를 설명한다. 해석으로 만든 승인 절차를 추가하지 않는다.
- 시작할 때 `git status --short`로 기존 변경을 확인한다. 사용자 변경·미추적 파일·다른 에이전트의 수정은 보존한다.
- 로컬 수정·검증과 원격 push·공개 배포를 구분한다. 이미 승인된 작업은 재승인을 요구하지 않는다.
- 검색은 대상 경로의 `rg` / `rg --files`부터 시작한다. `vendor/`, `_site/`, 대용량 자산 전체를 기본 검색 범위로 삼지 않는다.
- CLI 답변은 결과, 변경 파일, 검증, 남은 한계를 짧게 전달한다. 표와 목록은 비교·순서가 필요할 때 사용한다. 진행 중 추가 요청은 현재 목표에 반영한다.

## 필요한 지침만 읽기

| 작업 | 기준 문서 |
| --- | --- |
| 글 작성·큰 개정·서식·인용·본문 Figure | [blog-research-note](.agents/skills/blog-research-note/SKILL.md)의 해당 경로 |
| 새 글 또는 새 표현 형식 | [_posts/2026-04-08-template.md](_posts/2026-04-08-template.md)의 관련 예시 |
| Front matter·SEO·실험 출처 메타데이터 | [SEO.md](SEO.md) |
| 환경 설정·빌드·검증·발행 절차 | [WORKFLOW.md](WORKFLOW.md) |
| 에이전트 역할 변경 | 해당 `.codex/agents/*.toml`; 공통 글 규칙은 스킬에서 관리 |

글과 무관한 설정·스크립트 작업에는 글쓰기 스킬과 전체 템플릿을 불러오지 않는다. 작은 글 수정은 해당 부분과 연결된 예시만 확인한다. 새 재사용 컴포넌트를 추가했다면 템플릿에도 짧은 예시를 남긴다.

## 근거 보존

- 공개 연구 노트는 `문제 설정 → 평가 설계 → 관찰 → 해석 → 한계`를 드러낸다. 대상 글의 언어와 번역 연결을 유지한다.
- 실험 수치·비교 조건·방법 의미·결론 강도를 서식 정리 과정에서 바꾸지 않는다. 모순은 위치와 근거를 보고하고, 없는 결과나 인용은 만들지 않는다.
- 공개 범위를 확인한 근거만 사용한다. 출처 메타데이터는 `SEO.md`를 따르고, 본문·자산·주석에 내부 경로와 비공개 식별자를 옮기지 않는다.

## 병렬 작업

- 독립적인 확인 지점이 여럿이고 병렬화가 시간이나 품질에 도움이 되면 Sub Agent를 사용한다. 작은 단일 수정은 직접 처리한다.
- 위임할 때 목표, 입력 파일, 수정 또는 읽기 전용 여부, 소유 파일/영역, 완료 기준을 명시한다. 같은 글의 편집은 직렬화하거나 서로 다른 영역으로 나눈다.
- 초안은 `research_writer`, 서식은 `format_polisher`, Figure는 `visual_designer`, 출처는 `citation_checker`, 최종 발행 점검은 `publish_readiness_checker`에 맡긴다. 모든 역할을 매번 호출할 필요는 없다.
- 부모는 독립 작업을 계속하고 자식의 근거·변경·미확인 사항을 통합한다. 이미 유효한 검증은 재사용하고, 충돌이나 후속 수정이 있을 때만 다시 확인한다.
- 사용 가능한 런타임과 `.codex/config.toml`의 동시 실행 한도를 따른다. 커스텀 역할은 추가 위임 대신 부모에게 후속 작업을 전달한다.

## 완료 기준

변경 범위에 맞는 [WORKFLOW.md](WORKFLOW.md)의 검증을 통과하고 diff를 검토한다. 낮은 영향의 문서 수정에 구현을 그대로 반복하는 테스트를 만들지 않는다. 관련 검증이 통과하면 새 실패·변경·미해결 우려 없이 검사를 넓히거나 반복하지 않는다. 실행하지 못한 검증과 확인된 실패를 구분해 보고한다.
